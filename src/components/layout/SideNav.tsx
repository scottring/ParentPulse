'use client';

// SideNav is retired. The Surface handles all navigation through
// contextual links. The top bar (wordmark + user menu) and the
// floating pen are the only persistent UI elements.
//
// This component returns null to avoid breaking imports.
export default function SideNav() {
  return null;
}
