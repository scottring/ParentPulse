import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Note: allowedDevOrigins is not a valid Next.js config option
  // If you need to allow specific origins, configure CORS in your API routes
};

export default nextConfig;
